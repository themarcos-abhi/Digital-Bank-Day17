function filterDoctors(){const q=document.getElementById('doctorSearch').value.toLowerCase();document.querySelectorAll('.doctor').forEach(x=>x.style.display=x.dataset.search.includes(q)?'flex':'none')}
function openChat(){document.getElementById('chat')?.classList.toggle('show')}
function chatReply(e){e.preventDefault();const i=document.getElementById('chatInput'),m=document.getElementById('messages'),q=i.value.trim();if(!q)return;m.innerHTML+=`<p class="user">${escapeHtml(q)}</p>`;let a='I can help with booking, reports, login and support. For account-specific issues, please create a support ticket.';const t=q.toLowerCase();if(t.includes('book'))a='Open Doctors, choose a specialist, then select a date, time and mode.';else if(t.includes('report'))a='Sign in and open Medical Reports. Never share another patient’s report over chat.';else if(t.includes('login')||t.includes('password'))a='Use the login page or create an L1 access ticket. Never share a password or OTP.';else if(t.includes('emergency'))a='This demo does not provide emergency care. Contact your local emergency service or nearest hospital immediately.';m.innerHTML+=`<p class="bot">${a}</p>`;i.value='';m.scrollTop=m.scrollHeight}
function escapeHtml(s){const d=document.createElement('div');d.textContent=s;return d.innerHTML}